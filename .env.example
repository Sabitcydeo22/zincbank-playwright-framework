# ─────────────────────────────────────────────────────────────
#  ZincBank test environment
#  Copy this file to ".env" and adjust the values if needed:
#     cp .env.example .env          (macOS / Linux)
#     copy .env.example .env        (Windows)
#  The .env file is ignored by git and should never be committed.
# ─────────────────────────────────────────────────────────────

# Base URL of the ZincBank application (a simulated banking app from CYDEO).
BASE_URL=https://zincbank.cydeo.io

# Demo / test account provided by the ZincBank application.
TEST_USER=student01@zinc.test
TEST_PASSWORD=9pJolA7GBQec

# ── Optional settings ────────────────────────────────────────
# Uncomment to see the browser window while the tests run.
# HEADLESS=false
